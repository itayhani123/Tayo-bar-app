export type VatCalculation = { netRevenue: number; vatAmount: number; grossRevenue: number };
export type EventFinanceInput = { guestCount: number; pricePerGuest: number; vatRate: number; priceIncludesVat: boolean };
