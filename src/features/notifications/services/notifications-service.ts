import type { AppNotification } from "../types";async function parse<T>(response:Response):Promise<T>{const body=await response.json() as T|{error?:string};if(!response.ok)throw new Error((body as {error?:string}).error??"הפעולה נכשלה");return body as T;}export const listNotifications=()=>fetch("/api/notifications").then(r=>parse<AppNotification[]>(r));export const markNotificationRead=(id:string)=>fetch("/api/notifications",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id})}).then(r=>parse<{ok:boolean}>(r));export const markAllNotificationsRead=()=>fetch("/api/notifications",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({all:true})}).then(r=>parse<{ok:boolean}>(r));
