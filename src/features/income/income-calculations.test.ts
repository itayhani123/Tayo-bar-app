import test from "node:test";import assert from "node:assert/strict";import { calculateVat } from "@/lib/finance/calculations";
test("future events can be separated from actual revenue by date-only value",()=>{const today="2026-08-08";assert.equal("2026-08-09"<=today,false);assert.equal("2026-08-08"<=today,true);});test("event revenue changes automatically with guests and price",()=>{assert.equal(calculateVat({guestCount:100,pricePerGuest:15,vatRate:18,priceIncludesVat:true}).grossRevenue,1500);assert.equal(calculateVat({guestCount:200,pricePerGuest:20,vatRate:18,priceIncludesVat:true}).grossRevenue,4000);});
