export const normalizeHeader=(value:unknown)=>String(value??"").trim().replace(/[״׳"']/g,"").replace(/\s+/g," ").toLowerCase();
export function parseCurrency(value:unknown):number|null{if(value===null||value===undefined||value==="")return null;const cleaned=String(value).replace(/[₪,$€\s]/g,"").replace(/,/g,"");if(!/^-?\d+(\.\d+)?$/.test(cleaned))return null;const number=Number(cleaned);return Number.isFinite(number)?number:null;}
export function parseExcelDate(value:unknown):string|null{if(value instanceof Date&&!Number.isNaN(value.getTime()))return local(value);if(typeof value==="number"&&Number.isFinite(value)){const date=new Date(Date.UTC(1899,11,30)+Math.floor(value)*86400000);return `${date.getUTCFullYear()}-${String(date.getUTCMonth()+1).padStart(2,"0")}-${String(date.getUTCDate()).padStart(2,"0")}`;}const text=String(value??"").trim();const dmy=/^(\d{1,2})[/.\-](\d{1,2})[/.\-](\d{4})$/.exec(text);if(dmy)return `${dmy[3]}-${dmy[2].padStart(2,"0")}-${dmy[1].padStart(2,"0")}`;return /^\d{4}-\d{2}-\d{2}$/.test(text)?text:null;}function local(d:Date){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`;}
type DuplicateInput={expenseDate:string;supplierName:string;amount:number;referenceNumber:string};
export function possibleDuplicate(a:DuplicateInput,b:DuplicateInput){return a.expenseDate===b.expenseDate&&a.supplierName.trim().toLowerCase()===b.supplierName.trim().toLowerCase()&&a.amount===b.amount&&(!a.referenceNumber||!b.referenceNumber||a.referenceNumber===b.referenceNumber);}
export const isValidImportAmount=(amount:number)=>Number.isFinite(amount)&&amount>0;
